3. TypeScript-এ any, unknown, আর never — পার্থক্য

TypeScript এ any, unknown, আর never হচ্ছে Data Type কিন্তু একটু কনফিউজিং। ওগুলো আসলে কী করে, আর কখন কোনটা ব্যবহার করা উচিত? 

🔹 any - 

any হচ্ছে যেটা খুশি সেটা। 

মানে Variable এ যেকোনো টাইপের ভ্যালু হতে পারে—স্ট্রিং, নাম্বার, অবজেক্ট..। এমনকি যেকোনো অপারেশনও চালাতে পারবে।

কিন্তু সমস্যা হলো: টাইপ সেফটি নেই যে কারনে ডিবাগিং কঠিন হয়ে যেতে পারে। 

উদাহরণ:

let data: any = 10;
data = "Hello";
data = { x: 10 };

🔹 unknown — 

unknown দেখতে any-র মতো হলেও এটা অনেক বেশি সেফ।
তুমি ভ্যালু যাই দাও না কেন, TypeScript বলবে:

“আগে টাইপ চেক করো, তারপর ব্যবহার করো।”

এটা ভুল করা থেকে বাঁচায়।

উদাহরণ:

let value: unknown = "Hello";

if (typeof value === "string") {
  console.log(value.toUpperCase());
}

🔹 never — 

never এর কোনো ভ্যালু নেই।

যখন একটা ফাংশন সবসময় error দেয়, বা চলতেই থাকে(Infinity Loop), তখন TypeScript এ never ব্যবহার হয়.

উদাহরণ:

function throwError(message: string): never {
  throw new Error(message);
}

5. Union & Intersection Types – 

TypeScript এর মধ্য আমার প্রিয় ফিচার হচ্ছে Union & Intersection types। 

🔹 Union Type — 

সহজ কথায় variable একটা কিন্তু টাইপ একাধিক হতে পারে।

উদাহরণ:

function printId(id: number | string) {
  console.log("ID:", id);
}


এখানে id হয় নাম্বার হবে, না হয় স্ট্রিং—দুইটার যেকোনো একটা হবে।

🔹 Intersection Type — 

Intersection basically দুইটা টাইপকে merge করে।
মানে নতুন টাইপটাকে ওই দুই টাইপের সব প্রোপার্টিই থাকতে হবে।

উদাহরণ:

type Person = {
  name: string;
};

type Employee = {
  employeeId: number;
};

type Staff = Person & Employee;

const member: Staff = {
  name: "Rahim",
  employeeId: 12,
};